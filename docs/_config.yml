# ServiceNow Jobs Digest Jekyll Config
# No theme — using minimal Jekyll with custom CSS

title: ServiceNow Jobs Digest UK
description: Daily curated ServiceNow jobs in the UK with visa sponsorship filtering
baseurl: ""

# Plugins
plugins:
  - jekyll-feed

# Build settings
markdown: kramdown
highlighter: rouge

# Defaults
defaults:
  - scope:
      path: ""
    values:
      layout: default
